# Документация по миграции пайплайна Spring PetClinic

## Что было сделано (Этап 1)
1. **Перенос пайплайна в репозиторий**: Базовая логика пайплайна перенесена в файл `Jenkinsfile` в корне проекта (режим Pipeline script from SCM).
2. **Безопасность секретов**: Из кода полностью исключены любые захардкоженные учетные данные.
3. **Логирование сборки**: На этапе `Checkout` настроен нативный вывод хэша коммита (`git log -1 --oneline`).
